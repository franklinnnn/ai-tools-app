/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "oaidalleapiprodscus.blob.core.windows.net",
      "udmmamkjicrltdpdtcrt.supabase.co",
      "replicate.delivery",
    ],
  },
};

export default nextConfig;
